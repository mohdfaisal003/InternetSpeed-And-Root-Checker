package com.rooted.checker.app_base

import com.rooted.checker.base.BaseActivity

abstract class AppBaseActivity : BaseActivity() {

    override fun initComponents() {
        /* Initialize */
    }
}