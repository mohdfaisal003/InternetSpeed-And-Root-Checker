package com.rooted.deviceinfo.app_base

import com.rooted.deviceinfo.base.BaseActivity

abstract class AppBaseActivity : BaseActivity() {

    override fun initComponents() {
        /* Initialize */
    }
}